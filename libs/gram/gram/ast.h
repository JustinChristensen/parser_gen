#ifndef GRAM_AST_H_
#define GRAM_AST_H_ 1

#include <stdlib.h>
#include <stdio.h>

struct grammar_node {
};

struct header_node {
};

struct token_defs_node {
};

struct token_def_node {
};

struct rules_node {
};

struct rule_node {
};

struct alts_node {
};

struct rhses_node {
};

struct rhs_node {
};

#endif // GRAM_AST_H_


